#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

# Download SDK tools.
silent-sdkmanager "tools"
silent-sdkmanager "build-tools;$BUILD_TOOLS"
silent-sdkmanager "platform-tools"
silent-sdkmanager "platforms;android-$ANDROID_SDK"

${DIR}/update-sdk
