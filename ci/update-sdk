#!/usr/bin/env bash

CI_SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

# Update SDK dependencies.
${CI_SCRIPT_DIR}/silent-sdkmanager --update
# Accept licenses.
${CI_SCRIPT_DIR}/silent-sdkmanager --licenses
